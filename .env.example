# Copy to .env for local development. Never commit a real key.
# The server reads this at request time; without it /api/health reports
# live: false and POST /api/analyze returns 503 not_configured.
ANTHROPIC_API_KEY=
